<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FifthActivity"
    android:background="@color/bg"
    android:padding="50dp">

   <ImageView
       android:id="@+id/lostSthImg"
       android:layout_marginTop="30dp"
       android:layout_width="wrap_content"
       android:layout_height="195dp"
       android:layout_centerHorizontal="true"
       android:visibility="visible"
       app:srcCompat="@drawable/lost_phone_img" />


   <Button
       android:id="@+id/lostSthBtn"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_below="@id/lostSthImg"
       android:layout_centerHorizontal="true"
       android:layout_marginTop="30dp"
       android:paddingBottom="10dp"
       android:backgroundTint="@color/lightblue"
       android:fontFamily="@font/cairo_semibold"
       android:onClick="lostSthClicked"
       android:text="ضاع مني حاجة"
       android:textSize="30sp" />

   <ImageView
       android:id="@+id/foundSthImg"
       android:layout_width="wrap_content"
       android:layout_height="130dp"
       android:layout_below="@id/lostSthBtn"
       android:layout_centerHorizontal="true"
       android:layout_marginTop="50dp"
       android:visibility="visible"
       app:srcCompat="@drawable/found_phone_img" />

   <Button
       android:id="@+id/foundSthBtn"
       android:layout_width="220dp"
       android:layout_height="wrap_content"
       android:layout_below="@id/foundSthImg"
       android:layout_centerInParent="true"
       android:layout_marginTop="30dp"
       android:backgroundTint="@color/lightblue"
       android:fontFamily="@font/cairo_semibold"
       android:onClick="foundSthClicked"
       android:text="لقيت حاجة"
       android:textSize="30sp" />



</RelativeLayout>